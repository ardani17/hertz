import { NextRequest } from 'next/server'; import { HertzInAppNotificationService } from '@shared/services/hertzInAppNotificationService'; import { apiError, apiSuccess } from '@/lib/apiResponse'; import { getCurrentMember } from '@/lib/memberAuth'; export const dynamic = 'force-dynamic'; const service = new HertzInAppNotificationService(); export async function POST(request: NextRequest) { const user = await getCurrentMember(); if (!user) return apiError('AUTH_REQUIRED', 'Login member diperlukan', 401); const body = await request.json().catch(() => null); if (body && typeof body.id === 'string') await service.markRead(user.id, body.id); else await service.markAllRead(user.id); return apiSuccess({ read: true }); }
