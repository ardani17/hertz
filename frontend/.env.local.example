# Salin ke frontend/.env.local (gitignore) untuk login dev di localhost.
# Jangan commit file .env.local. Produksi tidak memakai file ini.

ALLOW_DEV_TELEGRAM_LOGIN=true
DEV_SKIP_MEMBERSHIP_CHECK=true
